[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gametheory-cw1"
version = "0.1.0"

dependencies = [
    "numpy",
    "pyglpk"
]

[tool.hatch.build.targets.wheel]
packages = ["utils"]