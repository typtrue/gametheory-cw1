[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gametheory-cw1"
version = "0.1.0"

dependencies = [
    "numpy",
    "scipy"
]

[tool.hatch.build.targets.wheel]
packages = ["utils"]